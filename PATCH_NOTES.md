# TriPin 패치노트

내부 배포(EAS `preview`) 기준. build = Android versionCode.
최신 항목이 위에 옵니다.

---

## build 7 — 2026-09-07

### 지도
- **당겨서 새로고침 제거** — 지도에서 아래로 당겨 새로고침하던 동작 삭제. 지도는 이동·확대 시
  자동으로 다시 불러오며, 이제 위→아래 드래그가 지도 이동에 그대로 반영됩니다.
- **현재 위치 버튼 추가** — 지도 우하단 버튼 → 현재 위치로 이동(줌 15) + 파란 점 표시.
  "확대하면 개별 기록" 안내는 좌하단으로 이동.

### 하단 탭
- **글 작성(+) 버튼 정중앙 정렬** — 중앙 + 버튼이 왼쪽으로 약 50px 치우쳐 있던 것을
  탐색·타임라인 정확히 가운데로 수정.

### 아이콘 · 정리
- 홈 화면 아이콘 아트가 너무 크던 것을 **축소 + 정중앙 정렬** (마스크 여백 확보).
- 미참조 스플래시 이미지 파일(`assets/splash-icon.png`) 제거.

---

## build 6 — 2026-09-07

### 아이콘
- **새 앱 아이콘** — 입체 지도 조각 + 정중앙에 꽂힌 핀. 발바닥(👣) 아이콘 대체.
  Android adaptive(전경/배경/모노크롬) 포함, 배경 크림톤.

### 기록 작성
- **최근 사진 빠른 첨부** — 작성 화면에 기기 최근 사진 스트립. 다이얼로그 없이 탭 한 번으로 첨부.
- **게시 성공 피드백** — 게시 완료 시 체크 애니메이션 + "게시 완료!".
- **사진 자동 최적화** — 업로드 전 긴 변 1600px 리사이즈 + JPEG 압축.
  용량 약 80% 감소(예: 1.3MB → 0.27MB), 업로드 속도 향상.

### 기타
- 로딩 화면 전반에 스켈레톤 UI 적용 확인, 미사용 스피너 헬퍼 제거.
- 신규 라이브러리: `expo-media-library`, `expo-image-manipulator`.

---

## build 5 — 2026-09-07

### 기록 작성 · 사진
- **사진 선택 UI 개편** — OS 기본 팝업 → 앱 톤 바텀시트(카메라 / 앨범, 여러 장 선택).
- **업로드 진행률** — 게시 버튼 위 진행 바 + "사진 업로드 NN%".
- **권한 안내** — 사진/카메라 권한이 막혀 있으면 "설정 열기" 안내 시트
  (기존에는 조용히 실패).

### 전반
- **이미지 로딩 개선** — 전 화면 이미지에 페이드 인 + 디스크 캐시(`expo-image`).
  빈칸에서 툭 나타나던 현상 제거.
- **햅틱 피드백** — 사진 추가/삭제, 위치 확정, 게시 성공·실패 시 진동.
- 신규 라이브러리: `expo-image`, `expo-haptics`.

---

## build 4 — 2026-09-07

### 브랜드
- 앱 이름 **발자국 → TriPin** — 홈 화면 라벨, 웹 탭 제목, 앱 내 표기, 푸시 알림 제목.
- 로그인 화면: 로고 👣 → 📍, 태그라인 "여행지를 핀으로 남기는 지도".

### 수정
- 위치 선택 모달의 "이 위치로 설정" 버튼이 하단 내비게이션 바에 가리던 것 수정
  (SafeArea 적용).

---

## build 3 — 2026-09-06

### 수정
- **게시 실패 수정 (중요)** — 사진이 있는 기록이 실기기에서 게시되지 않던 문제.
  Expo 전역 `fetch` 폴리필이 React Native 파일 파트를 지원하지 않아 발생 →
  네이티브 업로드를 `XMLHttpRequest` 로 전환. 401 시 토큰 갱신 후 1회 재시도 추가.

---

## build 2 — 2026-09-06

### 수정 · 진단
- 업로드 실패 시 원인이 담긴 에러를 화면·서버 로그에 노출하도록 개선.
- 배포 환경 스토리지 설정(`SUPABASE_URL`) 정정.
- `eas.json` `preview` 프로필에 `autoIncrement` 추가 →
  이제 앱을 지우지 않고 덮어쓰기 업데이트 가능.
